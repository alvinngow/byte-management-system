type User {
  id: ID!
  firstName: String!
  lastName: String!
  mobileNo: String!
  avatar: String!
  school: School
}

type CurrentUser {
  id: ID!
  email: EmailAddress!
  firstName: String!
  lastName: String!
  mobileNo: String!
  avatar: String!
  createdAt: DateTime!
  school: School
}